name: flutter_timer_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_bloc: ^8.1.4
  freezed_annotation: ^2.4.1
  flutter_svg: ^2.0.9
  go_router: ^13.2.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  freezed: ^2.4.7
  build_runner: ^2.4.8

flutter:
  uses-material-design: true

  assets:
    - assets/svg/clock.svg
  #   - images/a_dot_ham.jpeg
